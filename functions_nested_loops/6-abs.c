#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _abs - computes the absolute value of an integer
 * @n: An input character
 * Return: always 0.
 */
int _abs(int l)
{
int b;
b = abs(l);
return (b);
}
