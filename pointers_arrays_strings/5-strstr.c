#include <stdio.h>
#include "main.h"
char *_strstr(char *haystack,char *needle)
{
	while (*s != '\0')
	{
        if(*accept == *s)
        {
          return (s);
        }
     s++;
     }


return (NULL);
}
