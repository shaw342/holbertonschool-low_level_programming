#include <stdio.h>
#include "main.h"
char *_strpbrk(char *s, char *accept)
{
	unsigned int i;
	unsigned int r;

	while (s[i])
	{
		while (accept[r])
		{
			if (s[i] == accept[r]
			{
				break;
			}
			l++;

		}
			if(accept == '\0')
			{
				break;
			}
	i++;
	}

return (s);
}
