#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen -  function that returns the length of a string.
 *  @s: have srtings of str
 * Return: pc
 */

int _strlen(char *s)
{
	int pc = strlen(s);

return (pc);
}
